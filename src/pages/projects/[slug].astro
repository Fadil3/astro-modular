---
// Redirect old /projects/:slug to new /project/:slug route
export const prerender = false;

const { slug } = Astro.params;

if (!slug) {
  return Astro.redirect('/404');
}

// Redirect to new URL pattern
return Astro.redirect(`/project/${slug}`);
---
